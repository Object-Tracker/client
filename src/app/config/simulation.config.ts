export const SIMULATION_CONFIG = {
  // Interval between each simulation step in milliseconds
  intervalMs: 200,

  // Distance to move per step in meters
  stepMeters: 10,

  // Total number of steps in a simulation
  totalSteps: 30,
};
